#!/usr/bin/env bundle

source 'https://rubygems.org'

group :default do
  gem 'middleman', '~> 3.1.5'
end

group :development do
  gem 'rake', '~> 10.1.0'

  gem 'bundler', '~> 1.3.5'
end

group :development, :test do
  gem 'rspec', '~> 2.14.1'
end
#!/usr/bin/env bundle

source 'https://rubygems.org'

group :default do
  gem 'middleman', '~> 3.0.14'
end

group :development do
  gem 'rake', '~> 10.0.4'

  gem 'bundler', '~> 1.3.4'
end

group :development, :test do
  gem 'rspec', '~> 2.13.0'
end

group :assets do
  gem 'bourbon', '~> 3.1.8'
end
#!/usr/bin/env bundle

source 'https://rubygems.org'

group :default do
  gem 'middleman-core', '~> 3.3.6'
end

group :development do
  gem 'rake', '~> 10.3.2'
end

group :development, :test do
  gem 'rspec', '~> 3.1.0'
end
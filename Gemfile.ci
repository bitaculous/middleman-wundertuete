#!/usr/bin/env bundle

source 'https://rubygems.org'

group :default do
  gem 'middleman', '~> 3.2.2'
end

group :development do
  gem 'rake', '~> 10.1.1'

  gem 'bundler', '~> 1.5.2'
end

group :development, :test do
  gem 'rspec', '~> 2.14.1'
end